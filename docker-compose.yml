services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: high_api
      POSTGRES_PASSWORD: high_api_dev
      POSTGRES_DB: high_api
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
